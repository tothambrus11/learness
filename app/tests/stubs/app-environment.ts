/** SvelteKit's `$app/environment` outside SvelteKit: a build with a name. */
export const version = 'test-build';
export const browser = false;
export const dev = false;
export const building = false;
