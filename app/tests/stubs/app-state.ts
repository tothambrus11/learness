/** SvelteKit's `$app/state`, as a page at the root with no query. */
export const page = { url: new URL('http://localhost/'), params: {}, route: { id: null } };
export const navigating = null;
export const updated = { current: false };
